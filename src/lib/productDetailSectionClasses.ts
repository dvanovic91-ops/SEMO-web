/**
 * 제품 상세: 흰 카드 안(라벨 + 본문) 공통 안쪽 여백.
 * «Состав набора» / «Подробнее о наборе» 가로 정렬이 동일하게 보이도록 문자열을 공유한다.
 */
export const PRODUCT_DETAIL_WHITE_CARD_INNER =
  'box-border px-4 py-4 sm:px-6 sm:py-5';

/** «Box composition» / «More about box» 등 상단 라벨 — 굵기·자간·색 공통 */
export const PRODUCT_DETAIL_SECTION_KICKER_BASE =
  'font-semibold uppercase tracking-[0.14em] text-slate-500';

/** 기본 모바일·sm 크기 (tighterMobileComposeTitle 아닐 때) */
export const PRODUCT_DETAIL_SECTION_KICKER_SIZE = 'text-[0.7rem] sm:text-xs';
